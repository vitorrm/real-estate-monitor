class RealState {
  constructor({ id, img, url, price, location, size }) {
    this.id = id
    this.img = img
    this.url = url
    this.price = price
    this.location = location
    this.size = size
  }
}

export default RealState
